spring.application.name=bruebaBacken

# Configuración de MongoDB Atlas (Recomendado)
# Reemplaza la URI con tu cadena de conexión de MongoDB Atlas
spring.data.mongodb.uri=mongodb+srv://username:password@cluster.mongodb.net/btg_fondos?retryWrites=true&w=majority

# Configuración del servidor
server.port=8080

# Logging
logging.level.org.seti.bruebabacken=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

# Configuración de Jackson (para manejo de fechas)
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=America/Bogota

# Configuración de Email (Spring Mail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=juancgutierrezh@gmail.com
spring.mail.password=ojxc yaik uugn muts
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configuración de notificaciones
app.notifications.enabled=true
app.notifications.email.enabled=true
app.notifications.sms.enabled=false 